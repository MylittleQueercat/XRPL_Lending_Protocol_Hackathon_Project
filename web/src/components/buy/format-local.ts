// Re-exports of the shared formatters plus the one label this screen needs. The shared format
// module is owned by the scaffold and is not modified here.
export { formatPercent, formatShares, formatXrp, shortAddress, shortHash } from "@/lib/format";

export const unitLabel = "share units";
