{
  "name": "raise-xrpl",
  "version": "0.1.0",
  "private": true,
  "description": "Raise: Track 1 Vanilla foundation for a secondary market in XRPL vault shares",
  "type": "module",
  "engines": {
    "node": ">=24.11.1 <25"
  },
  "scripts": {
    "doctor": "tsx src/cli.ts doctor",
    "vanilla": "tsx src/cli.ts vanilla",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test",
    "vault:smoke": "tsx src/cli.ts vault-smoke",
    "position": "tsx src/read-cli.ts position",
    "transaction": "tsx src/read-cli.ts transaction"
  },
  "dependencies": {
    "xrpl": "5.2.0-beta.1"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
